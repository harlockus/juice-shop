name: Veracode Reporting API - Findings (Single App)

on:
  workflow_dispatch:
    inputs:
      application_name:
        description: "Veracode application profile name (exact match)"
        required: true
        default: "Juice-Shop-GitHub-Demo"
      last_updated_start_date:
        description: "Start date (YYYY-MM-DD). Use an earlier date for first run."
        required: true
        default: "2025-12-01"

permissions:
  contents: read
  actions: write

jobs:
  pull_findings_single_app:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests veracode-api-signing

      - name: Pull Findings via Reporting API (resolve app IDs, generate, poll, paginate, filter)
        env:
          VERACODE_API_ID: ${{ secrets.VERACODE_API_ID }}
          VERACODE_API_KEY: ${{ secrets.VERACODE_API_KEY }}
          VERACODE_API_BASE: ${{ secrets.VERACODE_API_BASE }}
          APPLICATION_NAME: ${{ inputs.application_name }}
          LAST_UPDATED_START_DATE: ${{ inputs.last_updated_start_date }}
        run: |
          python scripts/pull_reporting_findings_single_app.py

      - name: Upload output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: veracode-reporting-findings-single-app
          path: out/
